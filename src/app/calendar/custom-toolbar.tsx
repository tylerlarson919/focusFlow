import React from "react";
import { ToolbarProps } from "react-big-calendar";

const CustomToolbar: React.FC<ToolbarProps> = () => {
  return null; // Return nothing to effectively remove the toolbar
};

export default CustomToolbar;
